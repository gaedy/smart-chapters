# Middleware Configuration

## تم إعداد الـ Middleware لحماية الصفحات

### المسارات المحمية:
- `/` - الصفحة الرئيسية
- `/account` - صفحة الحساب
- `/book` - صفحات الكتب
- `/explore` - صفحة استكشاف الكتب
- `/favorites` - المفضلة
- `/library` - المكتبة
- `/notes` - الملاحظات
- `/settings` - الإعدادات
- `/stats` - الإحصائيات

### كيف يعمل:
1. **للمستخدمين غير المسجلين**: يتم إعادة توجيههم تلقائياً إلى `/sign-in`
2. **للمستخدمين المسجلين**: يمكنهم الوصول لجميع الصفحات المحمية
3. **عند محاولة الوصول لصفحات التسجيل أثناء تسجيل الدخول**: يتم إعادة توجيههم للصفحة الرئيسية

### التحقق من المصادقة:
- يستخدم الـ middleware دالة `auth()` من NextAuth
- كبديل، يتحقق من وجود session token في الـ cookies
- يضيف `callbackUrl` ليعود المستخدم لنفس الصفحة بعد تسجيل الدخول

### للتطوير:
- يمكن رؤية الـ logs في console عند تشغيل `npm run dev`
- يمكن إزالة الـ console.log statements في الإنتاج
